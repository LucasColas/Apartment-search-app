
# Rule of coding 
Respect SOLID programming principles.
When you create a new code you need check if it doesn't exist already (or if a similar function already exists).

# Tests
Test your code.
Run the CI (if there's any).
